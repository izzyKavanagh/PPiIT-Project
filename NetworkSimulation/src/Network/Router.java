package Network;

import java.util.HashMap;
import java.util.Map;

// Simulates a Router
public class Router {
	
	// HashMap to store routing table (connected devices and their IP addresses)
    private Map<String, Computer> routingTable = new HashMap<>();

    public void connectDevice(Computer device) {
        //method to connect device to router
    	//store devices & their IPs in routing table
    }
}
