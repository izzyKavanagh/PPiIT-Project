package Network;

import java.util.HashMap;
import java.util.Map;

// Simulates a Router
public class Router {
    private Map<String, Device> connectedDevices = new HashMap<>();

    public void connectDevice(Device device) {
        connectedDevices.put(device.getIpAddress(), device);
    }
}
